using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GetNormalisedPos : MonoBehaviour
{
    public float NormalisedPos;
    private float size;
    // Start is called before the first frame update
    void Start()
    {
    }

    // Update is called once per frame
    void Update()
    {

    }

    float GetXDelta(GameObject gObject)
    {
        return 0;
    }
}
